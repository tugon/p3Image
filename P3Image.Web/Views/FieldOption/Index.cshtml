<div class="row" ng-init="init()">
    <div class="col-md-12">
        <div class="panel panel-indigo">
            <div class=" panel-heading">
                <h2>FIELD OPTION</h2>
                <div class="panel-ctrls">
                    <a href="#" class="button-icon" uib-tooltip="new Field Option" tooltip-placement="left" ng-click="openEdit()"><i class="ti ti-file"></i></a>
                </div>
            </div>
        </div>
        <div class="panel panel-indigo">
            <div class=" panel-body">
                <table class="table">
                    <tr>
                        <th>
                            ID
                        </th>
                        <th>
                            FIELD ID
                        </th>
                        <th>
                            TEXT
                        </th>
                        <th>
                            VALUE
                        </th>

                    </tr>
                    <tr ng-repeat="fieldOption in fieldOptionList">
                        <td>
                            {{fieldOption.Id}}
                        </td>
                        <td>
                            {{fieldOption.FieldId}}
                        </td>
                        <td>
                            {{fieldOption.Text}}
                        </td>
                        <td>
                            {{fieldOption.Value}}
                        </td>
                        <td>
                            <div class="panel-ctrls">
                                <a href="#" class="button-icon" uib-tooltip="edit" ng-click="openEdit(fieldOption)">
                                    <i class="ti ti-pencil-alt"></i>
                                </a>
                                <a href="#" class="button-icon" uib-tooltip="remove" ng-click="remove(fieldOption,$index)">
                                    <i class="ti ti-trash red"></i>
                                </a>
                            </div>
                        </td>
                    </tr>


                </table>
                <uib-pagination total-items="paging.count" max-size="10" ng-model="currentPage" class="pagination m-n pull-right" boundary-links="true" rotate="false" ng-change="pageChanged()" previous-text="&lsaquo;" next-text="&rsaquo;" first-text="&laquo;" last-text="&raquo;"></uib-pagination>
            </div>
        </div>
    </div>
</div>
